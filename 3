'''3) Dado um vetor que guarda o valor de faturamento diário de uma distribuidora, faça um programa, na linguagem que desejar, que calcule e retorne:
• O menor valor de faturamento ocorrido em um dia do mês;
• O maior valor de faturamento ocorrido em um dia do mês;
• Número de dias no mês em que o valor de faturamento diário foi superior à média mensal.

IMPORTANTE:
a) Usar o json ou xml disponível como fonte dos dados do faturamento mensal;
b) Podem existir dias sem faturamento, como nos finais de semana e feriados. Estes dias devem ser ignorados no cálculo da média;'''
import json

def carregarDados(arquivo):

    with open(arquivo, 'r') as f:
        dados = json.load(f)
    return dados

def calcularMedia(dados):

    valores = [d for d in dados if d > 0]
    return sum(valores) / len(valores)

def calcularEstat(dados):
   
    menor = min(d for d in dados if d > 0)
    maior = max(dados)
    media = calcularMedia(dados)
    diasSuperiorMedia = sum(1 for d in dados if d > media)
    return menor, maior, diasSuperiorMedia

arquivo = 'faturamento.json'
dados = carregarDados(arquivo)
menor, maior, diasSuperiorMedia = calcularEstat(dados)
print(f"Menor valor de faturamento: {menor}")
print(f"Maior valor de faturamento: {maior}")
print(f"Número de dias com faturamento superior à média: {diasSuperiorMedia}")
